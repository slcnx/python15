	else子句：

		循环正常执行，会打印OK
			for i in range(5):
				print(i)
			else:
				print("ok")

		循环不正常执行，不会打印OK
			for i in range(100):
				if i > 50:
					break
			else:
				print("ok")


	练习：
		打印一个边长为n的正方形
		求100以内所有奇数的和(2500)
		判断学生成绩，成绩等级A-E。其中90分以上为A，80-90分为B, 70-79分为C, 60-69分为D, 60分以下为E：
		求1到5阶乘之和
		给一个数，判断它是否为素数			